---
name: lxmusic-batch-downloader
description: 大批量按歌单/清单下载无损歌曲+歌词。将只有「歌名+歌手」的文字或文件清单，批量变成电脑里的无损音乐文件（FLAC）+ 配套歌词（.lrc）。支持按分类自动归档、重命名整理、生成报告。适用于车载音乐批量下载、歌单收集、无损音乐批量获取等场景。需要先装好洛雪助手并导入自定义音源。
tags: [music, download, batch, flac, lrc, lx-music]
---

# 🎵 洛雪音乐批量下载器 Skill

## 📖 【给普通用户看】这是什么？怎么用？

### 一句话解释
这个工具能帮你把一份"歌名+歌手"的歌单，**全自动变成电脑里的无损音乐文件**。就像有个智能助手帮你去各大音乐平台搜歌、下载、存文件夹，你只管给名单就行。

### 使用前需要准备的东西（3步搞定）

| # | 准备步骤 | 怎么做 | 注意 |
|---|---------|--------|------|
| **①** | 安装洛雪助手 | 打开 https://lxmusic.toside.cn/ → 下载 Windows 版 → 安装打开 | 这是核心软件，必须装 |
| **②** | 开启开放 API | 洛雪 → **设置** → **开放 API** → 勾选"启用开放 API 服务" | 记下页面显示的**端口号**（通常是23330，也可能被改过） |
| **③** | 导入自定义音乐源 | 洛雪 → **设置** → **自定义源管理** → 添加 `.js` 音源文件 | 就是之前那个"墨澜音乐源 v2.3.0.js"这类聚合音源。音源可网上搜集，也可从 **[guoyue2010/lxmusic-](https://github.com/guoyue2010/lxmusic-)**（作者维护的洛雪自定义音源合集仓库，注意区分作者及其默认分支 main 下各版本目录）中获取 |

> 💡 **小贴士**：第②步最容易被忽略——不启用就下不了歌！记不清端口一定要打开软件看一眼再填。

### 我能帮到你什么？

✅ 输入一首首歌名和歌手 → 输出满文件夹的 `.flac` 无损音乐 + `.lrc` 歌词  
✅ 自动识别哪些是重复歌曲不浪费空间  
✅ 同歌手自动排在一起，方便查找  
✅ 如果第一平台没这首歌，自动换第二个搜，直到找到  
✅ 给你出一份详细的"成功/失败报告"

### 什么时候会失败？

- ❌ 还没安装洛雪 / 没启用开放 API / 没导入音源 → 先补齐这些
- ❌ 某些纯配乐（如动漫OST）、特别冷门曲目 → 可能搜不到，会在报告中列出
- ❌ 网络不通 → 检查联网情况

---

## 🔧 【给AI Agent看】标准化执行流程

> ⚠️ **安全红线（绝对不可违反）**：
> 1. **不删任何音频文件**——所有操作仅追加，不动已有文件
> 2. **只处理当前任务范围内的歌曲**——不碰其他目录
> 3. **不修改系统文件**——只在指定输出目录下工作
> 4. **尊重用户选择**——音质、目录、分类都以用户确认为准，不自作主张
> 5. **不窃取或传输任何用户数据**——所有操作在本地完成
> 6. **不修改用户原有配置**——不改洛雪的设置，只读取状态

### Step 0: 环境探测与确认

```
A. 确认洛雪是否运行（探测端口，默认 23330）
   - curl http://127.0.0.1:<PORT>/status → 应返回播放器状态JSON
   - 若不通：提醒用户打开洛雪并启用开放API，询问端口值

B. 确认自定义音源已导入
   - 检查洛雪设置中是否有自定义音源（通过桌面端UI查看或用户确认）
   - 若没有：要求用户提供音源 .js 文件或说明"就用现有音源"

C. 确认 Python 环境可用
   - python --version → 需要 3.11+
   - pip install requests pypinyin → 确保依赖就绪
```

### Step 1: 交互式提问（收集参数）

> 📋 AI 必须一次性向用户发出以下选择题，等用户回复后再继续：

```
请回答以下问题（直接给答案即可）：

Q1. 你的歌单长什么样？
   A) 粘贴文本（一行一首 "歌名-歌手"）
   B) 我有一个文件（md/csv/txt，告诉我路径）

Q2. 想下到哪个文件夹？（填完整路径，例：D:\我的车载音乐）

Q3. 要什么音质？（推荐选无损优先）
   [1] 纯无损 FLAC（最大音质）
   [2] 无损为主，没有就 320k MP3 ← 推荐
   [3] 尽量最高音质（母带级 flac24bit）
   [4] 标准 320k MP3（省空间）

Q5. 平台搜索优先级？（默认已设：酷狗 > QQ > 网易 > 酷我 > 咪咕）
   [1] 默认即可
   [2] 自定义（告诉我你的顺序）

Q6. 先用 3~5 首试跑一次，对再全量？
   [1] 先试跑 ← 推荐新手
   [2] 直接全量

Q7. （可选）你有 QQ/网易 VIP 吗？有就给我 cookie，解锁更高音质。
```

> 收到回复后：AI 填写 `USER_CONFIG` 字典中的对应字段。

### Step 2: 解析歌单

```python
# 支持的格式：
# 1. 行格式: "歌名-歌手" 或 "歌名 歌手"（空格分隔）
# 2. Markdown表格: |序号|歌名|歌手|年份|备注|
# 3. CSV/TXT: 简单列分隔
#
# 输出: list[(seq, name, singer, category)]
```

### Step 3: 下载引擎（核心循环）

对于每首歌曲 (name, singer)，执行以下逻辑：

```python
for song, singer in songs:
    for source in SOURCE_PRIORITY:  # kg > tx > wy > kw > mg
        cands = search(source, name, singer)
        if not cands:
            cands = search(source, name, "")          # 放宽：只按歌名搜
        
        hit = best_match(cands, name, singer, threshold=0.55)
        if not hit or score < threshold:
            continue                                  # 匹配度不够，跳过
        
        sid = get_song_id(hit, source)                # 提取歌曲ID
        url = get_url(sid, quality_order)             # 按flac24bit/flac/320k/128k重试
        lyric = get_lyric(sid, source)
        
        if url:
            save(url, path)                           # 下载音频
            save(lyric, path.lrc)                     # 保存歌词
            
            if quality == 'flac24bit' and size > 60MB:
                retry = get_url(sid, ['flac', ...])   # 大文件回退到flac
```

#### 关键算法说明
- **匹配校验**: 使用 `difflib.SequenceMatcher`，歌名 ≥ 0.55 且歌手匹配才算命中
- **兜底策略**: 歌名 + 歌手搜不到 → 只按歌名搜 → 去除后缀词（"主题音乐""伴奏"等）再搜 → 换平台
- **并发控制**: 线程池 max_workers=8~12，避免触发生效限流
- **断点续传**: 已存在的音频+.lrc 配对则跳过，不重下

### Step 4: 后处理（可选但推荐）

```python
# 4a. 按歌手相邻排序（拼音排序）
# 4b. 连续编号 (001, 002...)
# 4c. 清理孤立歌词（无对应音频的.lrc）
# 4d. 输出映射表 CSV（旧名→新名，用于还原）
```

### Step 5: 输出报告

```csv
序号,歌曲,歌手,分类,状态,来源,音质,大小MB,歌词
```

给用户的一句话总结：
```
共 N 首，成功 X 首（成功率 Y%），无损占比 Z%，缺失 M 首及原因。
报告已保存至：{report_path} / {missing_list_path}
```

---

## 🛡️ 隐私与安全规范（Agent必读）

| 原则 | 具体做法 |
|------|---------|
| **文件安全** | 仅在用户指定的输出目录内读写；绝不触碰其他目录 |
| **数据隐私** | 不上传/传输用户歌单、Cookie、端口信息到外部服务器 |
| **音量控制** | 下载并发不超过 12，避免触发音乐平台风控 |
| **可逆操作** | 所有重命名都有备份映射表；删除操作前先确认再送回收站 |
| **最小权限** | 只用 Cookie 做音质升级，绝不记录或持久化存储 |
| **用户知情** | 每个阶段都告诉用户正在做什么、进度如何 |

---

## 📝 快速参考卡（Agent备忘录）

```
端口地址: http://127.0.0.1:{用户填写的端口}/status
音源文件: 用户提供的 .js 或已导入的自定义源
质量顺序: flac24bit > flac > 320k > 128k
源优先级: kg(酷狗) > tx(QQ) > wy(网易) > kw(酷我) > mg(咪咕)
匹配阈值: 歌名≥0.55 + 歌手匹配
兜底策略: 常规搜索 → 只歌名 → 去后缀 → 换平台 → loose模式
输出格式: 编号-歌曲-歌手.flac/.mp3 + 同名.lrc
测试验证: 先跑 3~5 首确认链路通再全量
```

---

## 💬 给AI的一句话摘要

> 用户给歌单 → 确认洛雪+API+音源已就绪（缺就暂停补充）→ 发问卷收集音质/目录/优先级 → 跑 `lxmusic_batch_downloader.py` → 逐首搜索下载无损+歌词 → 同歌手相邻+连续编号 → 出报告。遇配乐的宽松搜索兜底。**绝对不删音频、不泄露数据。**

---

## 📦 配套资源

| 文件名 | 用途 |
|--------|------|
| `lxmusic_batch_downloader.py` | 主脚本（含USER_CONFIG模板，改几行即用） |
| `README.md` | 详细说明文档 |
| `LICENSE` | MIT 开源协议 |

> 本 Skill 基于实际部署经验编写，已通过多轮真实任务验证（518 首无损入库案例）。
